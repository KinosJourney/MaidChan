# Maid-chan 候选功能规划

> 本文用于记录未来可能开发的功能，不代表开发承诺或当前实现状态。正式排期前，应先核对现有代码、平台兼容性、美术资源、第三方依赖和 API 成本。

## 第一部分：基础稳健功能

特点：以本地“判定 + 触发”逻辑为主，通常稳定、延迟低，适合按需逐项实现。

### 1. 聊天记录监控与回溯（已完成）

- 在右键菜单中增加“历史记录”面板。
- 按时间戳清晰列出用户与角色的全部对话。
- 每条记录提供“删除”按钮。
- 删除时同步清除本地日志或内存中的对应记录并刷新列表，使该内容不再进入角色上下文。

### 2. 动态使用说明书（已完成）

- 在菜单栏增加“帮助 / 说明”入口。
- 点击后使用系统默认文本编辑器打开说明，或在程序内弹出 Markdown 渲染窗口。
- 内容从项目根目录的 `readme.md` 动态读取，以便无需修改代码即可更新功能介绍和快捷键。

### 3. 核心人设即时修改（已完成）

- 在设置界面提供多行文本框，展示当前加载的 System Prompt。
- 支持直接修改角色性格、称呼和语言风格。
- 保存后立即更新内存中的 Prompt，并重置短期对话上下文，无需重启程序。

### 4. 个人档案注入（已完成）

- 在菜单中增加“御主档案”填写区。
- 字段包括昵称、生日、对 OC 的称呼、关系设定等。
- 数据保存为 JSON。
- 每次调用对话 API 时，将档案信息拼接到 Prompt 前端，确保角色持续掌握用户身份和关系设定。

### 5. 整点报时与提醒

- 后台任务定期检查系统时间，并在整点触发一次报时事件。
- 可切换角色为“张嘴 / 喊话”状态并播放预设音效。
- 也可调用 API，按午夜、清晨、白天等时段生成简短报时气泡。
- 需防止同一整点被重复触发。

### 6. 专注时钟与监督

- 提供可配置的专注倒计时。
- 专注期间定期检测当前活动窗口标题或进程名。
- 检测到 Steam、视频网站等非工作类软件时，以气泡或窗口震动进行提醒。
- 倒计时结束后主动夸奖用户，并发放一定的虚拟货币奖励。
- 应提供应用黑白名单，避免误判。

### 7. 喂食与背包系统

- 使用 `items.json` 维护物品数据库，包括名称、图片路径、数量和对应回复文本。
- “背包”界面以网格形式展示物品。
- 使用食物后扣除数量，并播放进食动画。
- 可根据食物名称调用 API 生成口感评价；API 不可用时使用本地预设回复。

### 8. 好感度数值体系

- 维护全局整数 `affinity`，并设置等级和阈值。
- 抚摸、喂食、聊天等行为可增加好感度，同时设置每日上限或冷却时间以防刷取。
- 升级时解锁隐藏语音、待机图片或其他内容。
- 在状态栏显示爱心进度条。

### 9. 日程提醒助手

- 支持添加“时间 + 事项”的定时任务。
- 后台匹配系统时间，并可提前 5 分钟预警。
- 到时将角色窗口置顶，显示事项内容并播放提示音。
- 用户点击“知道了”后关闭提醒。
- 需处理重复日程、跨天任务和错过提醒等情况。

### 10. 随机事件小剧场

- 设置可调触发频率，例如每小时一次。
- 调用 API 生成情景描述和两个选项。
- 用户选择后执行简单的金币、好感度等数值变化，并显示结果。
- 可准备本地事件池，作为离线模式和 API 失败时的降级方案。

### 11. 闲聊陪伴模式

- 维护用户无操作计时器。
- 用户在设定时间内没有与电脑交互时，生成符合人设的关心或闲聊文本。
- 以桌面气泡静默展示，不弹窗打断工作。
- 在专注、游戏、勿扰和睡眠模式下自动停用。

### 12. 基础物理与交互

- 鼠标左键按住角色可自由拖拽。
- 松开后根据位置模拟重力下落至任务栏上方。
- 拖至屏幕左右边缘时自动吸附，并切换为“攀爬 / 挂靠”图片。
- 需兼容多显示器、不同缩放比例和任务栏位置。

### 13. 系统状态感知

- 使用 `psutil` 定期监控 CPU 和内存占用。
- CPU 持续高于阈值时切换为“晕倒 / 流汗”表情，并吐槽电脑过热。
- 内存紧张时提示“脑容量不够了”。
- 状态恢复后自动切回待机表情。
- 应使用持续时间和滞回阈值，避免表情频繁闪烁。

### 14. 剪贴板互动

- 监听系统剪贴板文本变化。
- 复制新文本时显示“你复制了什么？”提示图标。
- 点击后按内容长度或关键词给出本地吐槽，也可调用 API 分析。
- 提供“吃掉”按钮以清空剪贴板并增加好感。
- 默认避免读取密码管理器、敏感字段或非文本内容，并提供总开关。

### 15. 昼夜作息系统

- 根据系统时间切换角色状态。
- 例如 23:00 至次日 07:00 使用睡觉图片，并将点击反馈改为“呼噜呼噜……”或“Zzz...”。
- 早晨首次唤醒时触发“早安”事件。
- 睡眠时段和是否屏蔽常规对话应允许用户配置。

### 16. 程序快速启动器

- 在右键菜单中增加“管家服务”列表。
- 通过配置文件绑定浏览器、IDE、游戏等常用程序的路径。
- 点击后使用安全的进程启动方式打开程序，并显示“正在为您启动……”提示。
- 启动参数应使用列表传递，避免执行未经校验的任意命令。

### 17. 网站快捷访问

- 在菜单中增加“书签 / 传送门”功能。
- 通过配置文件维护网站名称与 URL。
- 使用 `webbrowser` 调用默认浏览器打开网站，并配合角色动作或提示。
- 仅允许 `http` 和 `https` 等安全协议。

### 18. 猜拳与掷骰子

- 提供两个完全本地运行、无需 AI 的小游戏。
- 猜拳：用户选择出拳，程序随机生成角色出拳并判断胜负，播放对应表情。
- 掷骰子：双方随机生成 1 至 100 的数字并比较大小。
- 可与金币、好感度或成就系统联动，但需限制重复奖励。

### 19. 本地文件整理

- 用户选择“杂乱目录”和“目标目录”。
- 按扩展名将图片、文档、压缩包等移动到分类文件夹。
- 完成后由角色汇报整理结果。
- 执行前提供预览、冲突处理规则和撤销记录，默认不覆盖同名文件。

### 20. 商城与经济系统

- 专注、完成日程和每日签到可获得金币。
- 商城可购买增加好感的道具、新衣服图片组或背景家具。
- 金币、库存和已解锁内容保存到本地 JSON。
- 数据写入应采用临时文件替换或备份，避免异常退出导致存档损坏。

### 21. 网络延迟感知

- 定期测试网络连通性和延迟。
- 高延迟或断网时显示“信号丢失 / 断网”图标或维修动作。
- 恢复后自动取消提示。
- 检测目标应可配置，避免只依赖单一网站，并限制请求频率。

### 22. 游戏模式侦测

- 维护游戏进程白名单。
- 检测到游戏运行时，自动静音闲聊、缩小角色或移动到屏幕角落。
- 游戏关闭后恢复此前状态。
- 不同操作系统的进程名称和权限差异需单独适配。

### 23. 心情 / 情绪系统

- 维护 `Happy`、`Normal`、`Sad` 等情绪状态或连续情绪值。
- 对话情感倾向、随机事件结果和系统状态可影响情绪。
- 情绪会调整 API Prompt，例如心情低落时回复更简短。
- 同时改变待机表情，并设置自然恢复机制，避免角色长期锁定在单一情绪。

### 24. 随手记 / 便签条

- 菜单中增加“记一笔”功能，弹出半透明输入框。
- 内容追加保存到本地 `notes.txt`，或保存为带时间戳的结构化数据。
- 可随机读取一条笔记进行复习或提醒。
- 应提供关闭随机复习、删除笔记和隐私保护选项。

### 25. 本地天气感知

- 接入轻量天气 API，每小时获取一次本地天气。
- 下雨时叠加雨伞 PNG 或雨景特效，晴天恢复普通状态。
- 缓存最近一次结果，断网时继续使用缓存并标记数据时间。
- 城市、定位方式、更新频率和 API Key 应可配置。

### 26. 彩蛋、隐藏指令与扩展互动

- 可加入点击次数、特定拖拽路径、节日或口令触发的彩蛋。
- 可扩展换装、悬浮吸附、走路、洗澡、抚摸、睡觉、成长和丰富表情。
- 在美术资源充足时，可制作类似 Galgame 的固定剧情和分支事件。
- 彩蛋触发条件与剧情进度可保存到本地存档。

## 第二部分：进阶复杂功能

特点：依赖额外环境、高延迟 API、系统能力或复杂状态管理，开发和维护成本较高，应单独设计、验证和发布。

### 1. 观察日记生成

- 维护滚动日志，记录最近 3 天的重要交互，如专注时长、喂食内容和心情变化。
- 用户点击“生成日记”后，将结构化摘要发送给 API。
- 以角色第一人称生成带日期的数百字日记，模拟其内心世界。
- 发送前应允许用户预览数据，并过滤隐私信息。

### 2. 本地 TTS 语音与口型同步

- 接入 GPT-SoVITS、VITS 等本地语音模型。
- 收到文本回复后生成并播放 WAV 音频。
- 实时分析音频振幅或音素时间轴，控制嘴部开合图片。
- 需重点处理模型部署、生成延迟、音频队列、中断播放和口型同步精度。

### 3. 屏幕视觉监控（Vision）

- 在快捷栏增加“看屏幕”按钮，仅在用户主动触发时截图。
- 使用 `pyautogui` 等工具截取当前屏幕，压缩后发送给支持视觉输入的模型。
- 将“这是我现在的屏幕，请吐槽或辅助我”等指令与图片一并提交，并展示建议。
- 发送前提供截图预览、区域选择和敏感信息遮挡功能。

### 4. 长期记忆（向量检索）

- 使用 ChromaDB 或轻量余弦相似度检索保存对话摘要向量。
- 新对话开始前检索最相关的历史记忆，并注入 Context。
- 需要设计摘要、分块、去重、过期、删除和记忆权重机制。
- 用户删除原始对话时，应同步删除相关摘要和向量，保证真正遗忘。

### 5. 媒体 / 音乐嗅探

- 调用操作系统媒体会话 API，或在能力受限时读取窗口标题。
- 媒体变更时获取歌名和歌手，并让角色进行点评。
- 需分别适配 Windows、macOS 等平台。
- 默认只读取媒体元数据，不记录完整播放历史，并提供隐私开关。

### 6. 实时语音通话

- 整合 VAD、Whisper 或其他 STT、LLM 与 TTS。
- 建立“快捷键说话 → 录音停止 → 转文字 → 发送模型 → 播放语音”的半双工管道。
- 后续可探索全双工、流式识别、流式生成和打断能力。
- 核心难点包括端到端延迟、回声消除、设备切换、并发状态和错误恢复。

### 7. 多角色联动

- 为多个桌宠进程提供本地通信机制，如本地端口、Unix Socket 或受控共享状态。
- 桌面上同时运行多个 OC 时，可读取彼此状态并触发对话、打闹等互动。
- 需要定义角色发现、消息协议、冲突处理、频率限制和进程退出清理机制。
- 本地端口默认只监听回环地址，并校验消息来源，避免暴露到局域网。

## 建议的公共基础设施

为减少各功能重复开发，可优先建设以下共用模块：

1. **统一配置与存档层**：负责 JSON 校验、原子写入、备份、版本迁移和恢复。
2. **事件总线**：统一处理整点、日程、闲置、系统状态、情绪变化等事件。
3. **任务调度器**：代替每项功能各自创建高频线程，集中管理定时任务和退出清理。
4. **角色状态机**：协调待机、说话、睡觉、进食、游戏、专注等状态，解决图片和动画冲突。
5. **通知管理器**：统一气泡、声音、置顶和窗口震动，并支持勿扰模式。
6. **权限与隐私设置**：集中控制剪贴板、截图、活动窗口、媒体信息和联网行为。
7. **API 服务层**：统一超时、重试、限流、缓存、取消和离线降级。
8. **跨平台适配层**：隔离 Windows、macOS 在进程、窗口、任务栏和媒体 API 上的差异。

## 推荐实施顺序

1. 先完成统一配置、存档、调度和角色状态机。
2. 优先开发纯本地的小功能，如便签、启动器、书签、小游戏和基础互动。
3. 再加入日程、专注、好感度、背包、商城、情绪等相互关联的系统。
4. 系统监控、剪贴板、游戏侦测等功能应同时补齐隐私开关和跨平台适配。
5. 最后独立验证 Vision、TTS、长期记忆、实时语音和多角色联动等高级功能。
